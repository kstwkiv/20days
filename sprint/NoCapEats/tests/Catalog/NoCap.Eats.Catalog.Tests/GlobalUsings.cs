// Theory:
// Global usings centralize the common test framework imports so each test file stays focused on behavior rather than boilerplate.
// Summary:
// This file ensures the Catalog manual tests can use xUnit attributes without repeating the same using directive in every file.
global using Xunit;
